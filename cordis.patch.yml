# dsh-sound bundle layer — applied when a profile lists this
# bundle（package.json 的 dsh.bundle.patch 指向本文件）。
# `name` 是包名（从 profile 的 node_modules 解析），不是相对路径。
- insert:
    - id: dsh-sound
      name: dsh-sound
